name: Deploy Mobile App

on:
  push:
    branches: [main]
    paths:
      - 'apps/mobile/**'
      - 'packages/shared/**'
      - 'pnpm-lock.yaml'

jobs:
  build:
    name: Build iOS App
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build on EAS
        working-directory: apps/mobile
        run: eas build --platform ios --non-interactive --no-wait

  submit:
    name: Submit to App Store
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      # Note: Submission would typically wait for build to complete
      # and then submit to App Store. This is a simplified example.
      - name: Submit to TestFlight
        working-directory: apps/mobile
        run: |
          echo "Build submitted to EAS. Check Expo dashboard for status."
          # eas submit --platform ios --latest --non-interactive
